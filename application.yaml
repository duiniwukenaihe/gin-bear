server:
  port: 8080
  name: "gin-bear"

database:
  type: "postgres"  # mysql 或 postgres
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "your-password"
  dbname: "gin_bear"

redis:
  addr: "localhost:6379"

jwt:
  secret: "your-secret-key"
