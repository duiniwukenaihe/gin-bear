apiVersion: v1
kind: Service
metadata:
  name: gin-bear
  labels:
    app.kubernetes.io/name: gin-bear
    app.kubernetes.io/part-of: gin-bear
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gin-bear
  ports:
    - name: http
      port: 8080
      targetPort: http
